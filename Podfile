# MediaPipe's iOS Task Vision SDK is distributed via CocoaPods.
# Run `xcodegen generate` first to produce GolfSwingAnalysis.xcodeproj,
# then `pod install` to wrap it into GolfSwingAnalysis.xcworkspace.
# See README.md for the full setup sequence.

platform :ios, '17.0'
use_frameworks!

target 'GolfSwingAnalysis' do
  pod 'MediaPipeTasksVision'
end
